package Gun07;

public class _07_StringStartsWith {
    public static void main(String[] args) {
        // StartsWith : bununla başlıyor mu?
        // sonuç true veya false

        String text = "MR Texnoloji";

        System.out.println("text.startswith(MR) = "+ text.startsWith("MR"));
        System.out.println("text.startswith(te) = "+ text.startsWith("te"));
        System.out.println("text.startswith(h) = "+ text.startsWith("h"));




    }
}
